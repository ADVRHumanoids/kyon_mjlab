services:
  dev:
    container_name: kyon_mjlab
    image: ghcr.io/advrhumanoids/kyon_mjlab
    stdin_open: true
    tty: true 
    entrypoint: /bin/bash
    network_mode: host
    restart: always
    ulimits: 
      core: -1
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw  # x11
      - ~/.ssh:/root/.ssh  # ssh
      - ..:/root/kyon_mjlab:rw  # this repo
    environment:
      - TERM=xterm-256color
      - DISPLAY
      - HHCM_FOREST_CLONE_DEFAULT_PROTO=https
      - NVIDIA_DRIVER_CAPABILITIES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
  
